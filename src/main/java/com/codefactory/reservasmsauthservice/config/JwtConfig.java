package com.codefactory.reservasmsauthservice.config;

import org.springframework.context.annotation.Configuration;

/**
 * Configuración específica para JWT.
 * Esta clase contiene solo configuración relacionada a JWT.
 * 
 * Los beans de autenticación (authenticationProvider, authenticationManager, passwordEncoder)
 * se definen en SecurityConfigBeans.java
 */
@Configuration
public class JwtConfig {
    // Configuración JWT será agregada aquí cuando sea necesaria
}